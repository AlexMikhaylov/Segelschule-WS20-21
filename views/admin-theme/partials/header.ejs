<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title><%= pageTitle %> | Segelschule</title>
    <!-- Tell the browser to be responsive to screen width -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="author" content="Alexander Mikhaylov">
    <!-- Favicon icon -->
    <link rel="icon" type="image/ico" sizes="16x16" href="assets/images/favicon.ico">
    <link rel="apple-touch-icon" sizes="180x180" href="assets/images/apple-touch-icon.png">
    <link rel="manifest" href="assets/site.webmanifest">
    <%- include('./inc/style') %>

    <style>
        #frm_neue_buchung label.error,
        #frm_neuer_kurs label.error,
        #frm_neuer_kunde label.error,
        #frm_neuer_ma label.error,
        #frm_neuer_kursplan label.error,
        #frm_neuer_kursplan2 label.error {
            color: red;
        }
    </style>
</head>

<body class="hold-transition sidebar-mini layout-fixed">
    <div class="wrapper">

<%- include('./inc/top-nav') %>

<%- include('./inc/sidebar') %>

        <!-- Content Wrapper. Contains page content -->
        <div class="content-wrapper">

            <% if(messages.erfolg) { %>
                <div class="alert alert-success">
                    <%= messages.erfolg %>
                </div>
            <% } if(messages.error) {%>
                <div class="alert alert-danger">
                    <%= messages.error %>
                </div>

            <% } %>